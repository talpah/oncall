{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Schedule</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Iscustom</th>
                <td>{{ entity.isCustom }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('schedule') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
